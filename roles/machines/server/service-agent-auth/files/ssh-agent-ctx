#!/usr/bin/env python3

"""Configure logging and start the contextual SSH-agent command wrapper."""

import logging

from agent_proxy.context import main


def run() -> int:
    logging.basicConfig(level=logging.INFO, format="%(name)s: %(message)s")
    return main()


if __name__ == "__main__":
    raise SystemExit(run())
