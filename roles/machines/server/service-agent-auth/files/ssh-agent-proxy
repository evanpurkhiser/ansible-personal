#!/usr/bin/env python3

"""Configure logging and serve one systemd-accepted agent connection."""

import logging

from agent_proxy.service import run_proxy


def main() -> int:
    logging.basicConfig(level=logging.INFO, format="%(name)s: %(message)s")
    return run_proxy()


if __name__ == "__main__":
    raise SystemExit(main())
